export const lifemodeIntroComposition = String.raw`HyperFrames compositions are HTML-native.

The runnable version of this intro lives at:
- hyperframes/index.html
- hyperframes/compositions/LifemodeIntro.html

This TSX file is kept as a source stub only so the requested path exists without importing anything from the main Lifemode app.`;

export default lifemodeIntroComposition;
